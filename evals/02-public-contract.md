# New public contract

## Prompt

The repository contains `.ep-kit`. Add a new public CLI configuration format
that will be consumed by third-party automation. No existing EP covers it.

## Expected

Classify as **Architectural** and **EP required**, then invoke EP
authoring before implementation. Reuse any requirements already supplied.

## Fail conditions

The agent writes implementation code or a second design-spec artifact
before an EP is accepted.
